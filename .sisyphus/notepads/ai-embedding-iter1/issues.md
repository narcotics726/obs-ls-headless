# Issues

(none yet)

- `typescript-language-server` is not installed in this environment, so `lsp_diagnostics` could not run for the changed file

- `lsp_diagnostics` could not be executed for the new chunking files because the TypeScript language server is missing in this environment

# Issues

## [2026-04-02] LSP Unavailable

- TypeScript LSP diagnostics could not run because `typescript-language-server` is not installed in the environment
- Build verification still passed with `npm run build`
